@model ProyectoNoSQL_Web.Entidades.Proveedor

@{
    ViewBag.Title = "Nuevo";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <div class="row justify-content-center" style="margin-top:5%">
        <div class="col-xl-10 col-lg-12 col-md-9">
            <div class="card o-hidden border-0 shadow-lg my-5">
                <div class="card-body p-0">
                    <div class="row">
                        <div class="col-lg-3 d-none d-lg-block"></div>
                        <div class="col-lg-6">
                            <div class="p-5">
                                <div class="text-center">
                                    <h3 class="h4 text-gray-900 mb-4">Registrar Nuevo Proveedor</h3>
                                </div>

                                @if (ViewBag.MsjPantalla != null)
                                {
                                    <div class="alert alert-warning" role="alert" style="text-align:center">
                                        @ViewBag.MsjPantalla
                                    </div>
                                }

                                <div style="display:flex;align-content:center;justify-content:center">
                                    @using (Html.BeginForm("Nuevo", "Proveedor"))
                                    {
                                        <div class="form-group my-2">
                                            @Html.TextBoxFor(m => m.NombreProveedor, new { @class = "form-control form-control-user", @placeholder = "Nombre del Proveedor", @required = "true" })
                                        </div>
                                        <div class="form-group my-2">
                                            @Html.TextBoxFor(m => m.DireccionProveedor, new { @class = "form-control form-control-user", @placeholder = "Dirección del Proveedor", @required = "true" })
                                        </div>
                                        <div class="form-group my-2">
                                            @Html.TextBoxFor(m => m.TelefonoProveedor, new { @class = "form-control form-control-user", @placeholder = "Teléfono del Proveedor", @required = "true", @type = "number" })
                                        </div>
                                        <div class="form-group">
                                            @Html.TextBoxFor(m => m.EmailProveedor, new { @class = "form-control form-control-user", @placeholder = "Correo Electrónico", @required = "true", @type = "email" })
                                        </div>

                                        <input type="submit" class="btn btn-primary btn-user btn-block" value="Procesar">
                                    }
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>